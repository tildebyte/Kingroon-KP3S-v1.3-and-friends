This is set up such that 'printer.cfg' reads all macros from their own files

Macro definition files are named e.g. 'foo_cfg'

This is to avoid an overwhelming list in browsers which look for '.cfg' files
